import 'package:flutter/material.dart';

class VideoMessageTile extends StatelessWidget {
  const VideoMessageTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
