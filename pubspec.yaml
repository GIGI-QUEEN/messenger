name: secure_messenger
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  local_auth: ^2.1.8
  qr_flutter: ^4.1.0
  image_picker: ^1.0.7
  firebase_storage: ^11.6.0
  uuid: ^4.3.3
  video_player: ^2.8.2
  chewie: ^1.4.0
  flutter_chat_types: ^3.6.2
  provider: ^6.1.1
  flutter_firebase_chat_core:
    git:
      url: https://github.com/GIGI-QUEEN/flutter_firebase_chat_core.git
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/images/
  uses-material-design: true
